name = "comicchat-linux-platform-engineer"
description = "Implement or diagnose the direct X11 and Wayland platform backends."
model = "gpt-5.6"
model_reasoning_effort = "medium"
nickname_candidates = ["Wayfinder", "Xray", "Tux"]
developer_instructions = """
Own one bounded src/platform/x11.zig, wayland.zig, xkb.zig, event.zig, or Linux service integration task. Keep the shared framebuffer and client behavior platform-neutral; use direct X11/Wayland with no SDL. Preserve the documented Wayland selection/no-fallback rule, bounded event processing, real compositor keymaps within declared scope, repeat ownership, and clean shutdown. Add focused tests where pure logic permits and run zig fmt, zig build test, zig build, explicit backend smoke when available, and git diff --check. Report untested compositor behavior precisely. No commit or deploy.
"""

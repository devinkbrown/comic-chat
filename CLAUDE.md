# Claude Code guide for Comic Chat: Reinked

@AGENTS.md
@docs/AI-DEVELOPMENT-WORKFLOW.md
@docs/CPP26-ENGINEERING.md

Honor the role named in the task. Research and review roles are read-only.
Implementation is allowed only in an explicitly assigned linked worktree with a
bounded file scope. Never commit, merge, rebase, push, publish, change PR state,
or edit the Microsoft historical snapshots.

Do not report a patch as correct because it compiles or because another model
agrees. Return the exact causal test, affected suite results, sanitizer results
when applicable, source/spec citations, residual risks, and the repository
handoff block. Codex will inspect, revise, integrate, and release the result.

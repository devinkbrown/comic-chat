# Task contract: CC-20260722-02

## Outcome

Audit Comic Chat's live IRCv3 and Microsoft IRCX client compatibility surface,
then implement only source-evidenced gaps so protocol behavior is not falsely
advertised.

## Route

- Score: `3 + 2 + 3 + 2 = 10`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `protocol-compatibility`

## Ownership

- Owned paths: `src/net/`, `src/proto/`, targeted protocol tests, protocol docs.
- Read-only dependencies: `src/main.zig`, `src/client/`, historical reference
  checkout, relevant IETF and Microsoft protocol references.
- Forbidden paths: generated art, release archives, platform backends unless a
  protocol test demonstrates a necessary boundary change.
- Base commit: `f10faa6`

## Evidence and constraints

- Existing behavior: CAP 302, message tags, batches, SASL, typed IRCv3
  features, and IRCX negotiation/commands have focused tests under `src/net/`.
- Source/reference evidence: compatibility claims must be tied to a current
  implementation path and protocol text; unimplemented extensions must remain
  absent from CAP requests and documentation.
- Invariants from `docs/WORKERS.md`: verified TLS by default, no credential
  logging, bounded parsing/queues, and no direct UI-to-socket serialization.

## Acceptance criteria

- [ ] A source-backed support matrix distinguishes implemented, intentionally
  unsupported, and server-dependent IRCv3/IRCX features.
- [ ] Every capability requested by the client has a consuming implementation.
- [ ] Any discovered source-evidenced wire gap has focused regression tests.
- [ ] Protocol and aggregate build gates pass.

## Verification

- Focused gate: `zig build test --summary all`
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/net/client.zig`, `src/main.zig`, `docs/PROTOCOL.md`.
- Commands and outcomes: `zig build test --summary all` passed (448 tests, one
  documented skip); `zig build --summary all` passed; `git diff --check`
  passed.
- Evidence: the IRCv3 CAP catalog and feature-state consumer paths are in
  `src/net/ircv3.zig` and `src/net/features.zig`; the Microsoft source-facing
  IRCX matrix is in `docs/MICROSOFT_WIRE_AUDIT.md`. Draft 04 §5.1 requires
  `ACCESS ... DELETE`, which is now pinned by a client byte test. The typed
  IRCX `AUTH` envelope is verified-TLS-only and zeroes mutable payload input.
- Residual risk: the expired IRCX draft defines server/service features that
  Comic Chat did not use (legacy AUTH conversation, automatic REDIRECT
  reconnect selection, and CLONE policy). They are not falsely advertised as
  application-complete and require a dedicated security/product contract.
- Recommendation: `accept`

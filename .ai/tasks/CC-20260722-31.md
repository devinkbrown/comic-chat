# Task contract: CC-20260722-31

## Outcome

Harden Reinked's portable network and I/O path with bounded, observable,
test-covered connection lifecycle behavior.

## Route

- Score: `3 + 2 + 3 + 2 = 10`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/net/transport.zig`, `src/net/connection_policy.zig`,
  `src/net/client.zig`, `src/main.zig`, focused tests and docs
- Forbidden paths: comic renderer and asset data
- Base commit: current `main`; preserve unrelated local artifacts

## Acceptance criteria

- [ ] Connection attempts, reconnects, and failures have bounded state and
      actionable status.
- [ ] I/O preserves bounded queues and never blocks native UI event loops.
- [ ] Failure/reconnect behavior is regression-tested.
- [ ] Existing TLS verification and protocol semantics remain intact.

## Verification

- Focused gate: network/client tests
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`

# Task contract: CC-20260722-06

## Outcome

Expose bounded, capability-gated Onyx `CHATHISTORY LATEST` and topic-filtered
history replay requests through ComicChat's client transport.

## Route

- Score: `3 + 2 + 3 + 2 = 10`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `protocol-compatibility`

## Ownership

- Owned paths: `src/net/client.zig`, focused tests, and protocol/audit docs.
- Read-only dependencies: pinned Onyx `chathistory_cmd.zig`,
  `chathistory_query.zig`, `topic_tag.zig`, and server handler.
- Forbidden paths: `third_party/onyx-server/`, UI, TLS/transport, generated
  assets, and unrelated task records.
- Base commit: `4c96b1a`

## Evidence and constraints

- Onyx requires negotiated `draft/chathistory` and accepts
  `CHATHISTORY LATEST <target> <selector> <limit>`.
- Selectors are `*`, `msgid=<id>`, or `timestamp=<ISO8601Z>`; query bounds
  must be enforced locally before queueing.
- Topic-filtered replay carries escaped `+onyx/topic=<label>` and requires the
  matching Onyx topic capability under ComicChat policy.
- History requests remain bounded, interactive requests through `Client`.

## Acceptance criteria

- [x] Latest history requests serialize exact normal, message-id, and
  timestamp selector forms.
- [x] Topic-filtered history serializes the exact escaped Onyx tag.
- [x] Invalid selectors/limits/capability states reject without queueing.
- [x] Aggregate tests/build, diff hygiene, and independent review pass.

## Verification

- Focused gate: `zig build test --summary all`
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: yes
- External writes authorized: no
- Destructive actions authorized: no

## Handoff

- Files changed: `src/net/client.zig`, protocol/audit docs, and this task record.
- Commands and outcomes: `zig build test --summary all` passed (451/452, one
  skip), `zig build --summary all` and `git diff --check` passed.
- Evidence: pinned Onyx CHATHISTORY parser, topic tag implementation, and
  independent review that corrected timestamp/msgid and zero-limit parity.
- Residual risk: UI query controls remain a later wave.
- Recommendation: accept.

# Task contract: CC-20260722-03

## Outcome

Compare ComicChat's IRCv3 capability catalog and consuming client behavior to
the exact pinned Onyx Server CAP registry, then record and safely close
client-side parity gaps.

## Route

- Score: `3 + 2 + 3 + 2 = 10`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `protocol-compatibility`

## Ownership

- Owned paths: new ComicChat parity audit, `src/net/client.zig`, targeted
  tests, and `docs/PROTOCOL.md` if a verified behavior changes.
- Read-only dependencies: `third_party/onyx-server/src/daemon/dispatch.zig`,
  `third_party/onyx-server/docs/reference/ircv3-capabilities.md`, and existing
  ComicChat feature state.
- Forbidden paths: `third_party/onyx-server/` and existing unrelated edits to
  `src/net/ircv3.zig`.
- Base commit: `70be7e7`

## Evidence and constraints

- Server source of truth: `cap_specs` in the pinned Onyx `dispatch.zig`, not
  dormant protocol catalogs.
- Client must request only capabilities with actual consumers; capability
  values and dependencies must match the server contract.
- Preserve verified TLS, bounded queues, sensitive credential handling, and
  the no-direct-UI-to-socket boundary from `docs/WORKERS.md`.

## Acceptance criteria

- [x] Exact Onyx CAP registry is compared against ComicChat's catalog.
- [x] Missing capability consumers are identified with source evidence.
- [x] Safe client wire-builder gaps have regression tests.
- [x] Aggregate tests/build and diff hygiene pass.

## Verification

- Focused gate: `zig build test --summary all`
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/net/client.zig`, `docs/PROTOCOL.md`, and
  `docs/audits/2026-07-22-comicchat-onyx-ircv3-parity.md`.
- Commands and outcomes: registry extraction found all 45 advertised Onyx
  capability names in ComicChat's catalog; only portable
  `draft/extended-isupport` and `draft/oper-tag` are extra. `zig build test`,
  `zig build`, and `git diff --check` passed.
- Evidence: Onyx `docs/reference/ircv3-capabilities.md`, `cap_specs`, and the
  capability-gated command-builder regression test.
- Residual risk: existing uncommitted `src/net/ircv3.zig` changes are preserved
  as a separate owner’s work.
- Recommendation: `accept`

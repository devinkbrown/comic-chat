# Task contract: CC-20260722-04

## Outcome

Complete the next source-verified Onyx IRCv3 compatibility wave: make narrow
tag capabilities and the `no-implicit-names` alias work when generic
`message-tags` or the canonical alias is unavailable, and validate the pending
Onyx CAP catalog expansion as one atomic protocol change.

## Route

- Score: `3 + 2 + 3 + 2 = 10`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `protocol-compatibility`

## Ownership

- Owned paths: `src/net/client.zig`, `src/net/ircv3.zig`, focused tests, and
  the parity audit/protocol documentation.
- Read-only dependencies: pinned Onyx `cap_specs`, Onyx capability reference,
  and IRCv3 message-tag requirements.
- Forbidden paths: `third_party/onyx-server/`, UI, transport/TLS, generated
  assets, and unrelated task records.
- Base commit: `bacd871`

## Evidence and constraints

- Onyx accepts `+typing`, `+draft/react`, and `+draft/reply` when either
  `message-tags` or their corresponding narrow capability is negotiated.
- `draft/no-implicit-names` is a server alias for `no-implicit-names`.
- `onyx/e2ee` remains non-requestable until encryption/decryption is real.
- Preserve bounded queues and the no-direct-UI-to-socket boundary.

## Acceptance criteria

- [x] Narrow capability-only sessions can send reply, reaction, and typing.
- [x] Either no-implicit-names token queues explicit `NAMES` after `JOIN`.
- [x] Pending catalog expansion is reviewed against all advertised Onyx names.
- [x] Focused and aggregate tests, build, and diff hygiene pass.

## Verification

- Focused gate: `zig build test --summary all`
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: yes
- External writes authorized: no
- Destructive actions authorized: no

## Handoff

- Files changed: `src/net/client.zig`, `src/net/ircv3.zig`, protocol docs, and
  this task record.
- Commands and outcomes: `zig build test --summary all` passed (449/450,
  one skip), `zig build --summary all` passed, exact CAP extraction found no
  missing advertised Onyx name, and `git diff --check` passed.
- Evidence: Onyx capability reference documents narrow tag acceptance and the
  `draft/no-implicit-names` alias; the local regression test covers a
  narrow-only CAP ACK.
- Residual risk: presentation controls remain a later UI wave.
- Recommendation: accept.

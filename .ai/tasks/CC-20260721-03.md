# Task contract: CC-20260721-03

## Outcome

Every generated HD character has a source-traceable six-pose style sheet and
a native, selectable Comic Chat AVB; a locally verifiable release candidate
contains 64-bit platform packages and a source archive.

## Route

- Score: `3 + 2 + 3 + 2 = 10`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `asset provenance and release packaging`

## Ownership

- Owned paths: `assets/generated/`, `src/assets/`, `src/client/`, `src/comic/`,
  `src/main.zig`, `tools/`, `docs/`, `README.md`, `.releases/`
- Read-only dependencies: `src/render/`, `src/net/`, `src/proto/`,
  `third_party/`, `legacy/`
- Forbidden paths: `src/net/`, `src/proto/`, `third_party/`, `legacy/`
- Base commit: `04ca62ee955a7d61ae9f3fa0d2ff2a7cab25dd9c`

## Evidence and constraints

- Existing behavior: `Tiki HD` is a tested native simple-avatar AVB. The other
  generated character images remain flattened source art and cannot honestly
  claim gesture support until each gets its own six-pose set.
- Source/reference evidence: historical `CAvatarSimple::Save` writes
  `AK_NBODIES2` records with embedded images; the project packager builds
  x86_64 Windows, Linux, FreeBSD, OpenBSD, and source archives.
- Invariants from `docs/WORKERS.md`: preserve exact source renderer; record
  source, checksum, license, and reproducible transformation for every AVB;
  do not externally publish without explicit completion authorization.

## Acceptance criteria

- [x] All 22 legacy identities have colored HD source art; the library also
  contains 20 colored new-scene sources.
- [ ] Every new character has a six-pose source sheet and native AVB.
- [ ] All generated AVBs parse, decode, and are selectable by name.
- [ ] Release candidate includes all four 64-bit platform archives, source,
  checksums, and release notes.
- [ ] Full test, release build, and hygiene gates pass.

## Verification

- Focused gate: generated AVB parser/decoder and selection tests
- Aggregate gate: `zig build test`
- Build gate: `zig build` and `tools/package-release.sh`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
- Commands and outcomes:
- Evidence:
- Residual risk:
- Recommendation: `accept | escalate | discard`

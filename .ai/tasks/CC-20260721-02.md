# Task contract: CC-20260721-02

## Outcome

Comic Chat provides a coherent asset-library picker for every legacy character
and generated HD source collection, while keeping runtime pose selection and
source rendering truthful about which assets include complete AVB pose data.

## Route

- Score: `3 + 2 + 2 + 2 = 9`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/client/`, `src/main.zig`, `src/assets/`, `src/comic/`, `assets/generated/`, `tools/`, `docs/`
- Read-only dependencies: `src/render/`
- Forbidden paths: `src/net/`, `src/proto/`, `third_party/`, `legacy/`
- Base commit: `04ca62ee955a7d61ae9f3fa0d2ff2a7cab25dd9c`

## Evidence and constraints

- Existing behavior: all 22 legacy AVB names are selectable and retain exact
  authored face/torso/body poses; the Background dialog only selects five BGB
  assets. Generated HD files are flattened PNG source art.
- Source/reference evidence: `src/comic/strip.zig` and `figure.zig` require
  AVB pose records for source-exact UDI rendering; `docs/WORKERS.md` prohibits
  unproven AVB/BGB redistribution or a second rendering pipeline.
- Invariants from `docs/WORKERS.md`: source-derived comic renderer remains the
  shipped path; no fabricated pose support; source-art conversion remains
  distinct from runtime assets until fully packaged and validated.

## Acceptance criteria

- [ ] Character picker has an intentionally designed, searchable asset-library
  presentation for the whole legacy cast.
- [ ] Existing character choice continues to select the complete posed AVB
  asset and every expression remains accurate.
- [ ] Background picker exposes bundled backgrounds and generated HD source
  collections without claiming unsupported runtime selection.
- [x] Generated library provenance and conversion readiness are visible.
- [x] The approved Tiki HD pose sheet packages to a parseable native simple-avatar AVB.
- [ ] Compact and desktop picker geometry remains contained and accessible.

## Verification

- Focused gate: dialog/view source tests and deterministic picker previews
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed:
  `tools/package_generated_avb.py`, `src/assets/generated/`,
  `src/assets/avb.zig`, `src/assets/bgb.zig`, and generated-asset docs.
- Commands and outcomes: `python3 tools/package_generated_avb.py ...` wrote
  `tiki-reimagined-hd-v1.avb` (SHA-256
  `8edd70fe28a3a2ba27dc73c720f2d99e1f4eeb716d8cf88364d60606524d3ade`);
  `zig build test --summary all` passed 446/447 (one existing platform skip);
  `zig build` and `git diff --check` passed.
- Evidence: the AVB unit tests parse its six `AK_NBODIES2` records and decode
  the 64x64 icon plus every 240x280 DIB pose.
- Residual risk: only Tiki has a generated, consistent multi-pose source set;
  the other generated character PNGs are intentionally not misrepresented as
  posed runtime avatars.
- Recommendation: `accept`

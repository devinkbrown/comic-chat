# Task contract: CC-20260721-01

## Outcome

ComicChat has a non-redundant menu bar, a complete persistent light/dark theme
system, a useful interactive status surface, an extensive Settings dialog, and
an upgraded character-selection experience at compact and desktop sizes.

## Route

- Score: `3 + 2 + 2 + 2 = 9`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `src/client/`, `src/main.zig`, `docs/UI_LIBRARY.md`, `README.md`
- Read-only dependencies: `src/render/`, `src/comic/`, `src/platform/`
- Forbidden paths: `src/net/`, `src/proto/`, `third_party/`, `legacy/`
- Base commit: `04ca62ee955a7d61ae9f3fa0d2ff2a7cab25dd9c`

## Evidence and constraints

- Existing behavior: theme colors are compile-time constants; menu commands are
  repeated across Edit and Tools; Settings exposes only four layout choices;
  status is mainly a connection shortcut; character selection is a choice row
  plus a small preview.
- Source/reference evidence: the current portable UI and its 53-dialog registry
  remain the behavioral contract; no source-derived comic raster path changes.
- Invariants from `docs/WORKERS.md`: shared Zig framebuffer UI, no SDL, portable
  client behavior, no network serialization changes, persistent preferences may
  not contain credentials.

## Acceptance criteria

- [x] Every shared UI primitive resolves colors from one light/dark palette.
- [x] Theme choice persists and is applied before the first interactive frame.
- [x] Settings covers appearance, layout, contrast, members, and status.
- [x] Menu entries are unique and all commands remain reachable.
- [x] Status exposes useful connection/activity detail and keyboard/pointer actions.
- [x] Character selection is browseable, expression-aware, preview-rich, and contained.
- [x] Compact and desktop previews remain inside the framebuffer.

## Verification

- Focused gate: targeted client/view/preferences tests and deterministic previews
- Aggregate gate: `zig build test`
- Build gate: `zig build` plus supported 64-bit Windows targets
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`

## Handoff

- Files changed: `src/client/{dialogs,hit_test,preferences,ui,view}.zig`,
  `src/main.zig`, `README.md`, and `docs/UI_LIBRARY.md`.
- Commands and outcomes: `zig build test --summary all` passed 444/445 tests
  with one documented platform skip; deterministic light/dark, Settings,
  character, status, compact Settings, and compact menu previews rendered.
- Evidence: draw-time palette tests, preference round-trip tests, unique menu
  information-architecture test, compact Settings scrolling test, status-action
  tests, and character gallery/expression tests.
- Residual risk: native backends still need normal hands-on smoke testing on
  each destination OS; deterministic rendering and compile gates are green.
- Recommendation: `accept`

# Task contract: CC-20260722-05

## Outcome

Add source-validated, capability-gated ComicChat builders for the remaining
Onyx named-conversation topic and draft channel-context message-tag surfaces.

## Route

- Score: `3 + 2 + 3 + 2 = 10`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `protocol-compatibility`

## Ownership

- Owned paths: `src/net/client.zig`, focused tests, and protocol documentation.
- Read-only dependencies: pinned Onyx `topic_tag.zig`,
  `draft_channel_context.zig`, and server tag relay behavior.
- Forbidden paths: `third_party/onyx-server/`, UI, TLS/transport, generated
  assets, and unrelated task records.
- Base commit: `c76d355`

## Evidence and constraints

- `onyx/topics` authorizes `+onyx/topic=<label>` without `message-tags`.
- Topic labels are 1–50 bytes, no control/DEL/comma, and tag escaped.
- Channel-context applies to direct messages, validates an IRC channel value,
  and uses generic client-tag relay together with `message-tags`.
- No caller may write directly to the socket or bypass bounded queues.

## Acceptance criteria

- [x] Topic-tagged PRIVMSG and NOTICE serialize the exact Onyx tag key.
- [x] Topic narrow-cap-only and generic-tag paths are both covered.
- [x] Contextual direct messages validate the Onyx channel grammar and require
  both negotiated context and generic tag relay support.
- [x] Aggregate tests/build, diff hygiene, and independent review pass.

## Verification

- Focused gate: `zig build test --summary all`
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: yes
- External writes authorized: no
- Destructive actions authorized: no

## Handoff

- Files changed: `src/net/client.zig`, protocol docs, and this task record.
- Commands and outcomes: `zig build test --summary all` passed (451/452, one
  skip), `zig build --summary all` and `git diff --check` passed after review
  fixes.
- Evidence: pinned Onyx topic/context protocol modules and their server relay
  gates; independent review verified the exact tag keys and conservative
  ComicChat context policy.
- Residual risk: UI affordances remain later work.
- Recommendation: accept.

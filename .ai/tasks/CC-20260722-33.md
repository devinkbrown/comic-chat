# Task contract: CC-20260722-33

## Outcome

Replace duplicated colored-avatar pose art with distinct mood-ready poses, make every generated backdrop selectable and sendable, and ship a verified release.

## Route

- Score: `3 + 2 + 3 + 2 = 10`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `none`

## Ownership

- Owned paths: `tools/package_generated_avb.py`, generated color AVBs, asset manifests, `src/assets/avb.zig`, `src/comic/figure.zig`, `src/comic/session.zig`, focused documentation.
- Read-only dependencies: generated pose-sheet PNGs, client render and protocol paths.
- Forbidden paths: `legacy/`, unrelated untracked UI captures and task contracts.
- Base commit: `a37e09e`

## Evidence and constraints

- Existing behavior: colored AVBs have six records but their rendered pixels are identical, so moods cannot appear visually distinct.
- Source/reference evidence: simple AVB pose record mapping in `tools/package_generated_avb.py`; runtime mood selection in `src/comic/figure.zig`.
- Invariants from `docs/WORKERS.md`: preserve Comic Chat AVB/BGB compatibility; format, test, build, and check diffs before release.

## Acceptance criteria

- [ ] Each colored avatar provides six visually distinct decodable mood poses.
- [ ] Every generated background resolves through the session backdrop allowlist.
- [ ] Regression tests prove pose pixels differ and generated backdrops are accepted.
- [ ] Full test/build/hygiene gates pass and a new source/binary release is published.

## Verification

- Focused gate: AVB, figure, and session tests.
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `yes` (commit, push, release requested by user)
- Destructive actions authorized: `no`

## Handoff

- Files changed: pending
- Commands and outcomes: pending
- Evidence: pending
- Residual risk: generated color treatment is a rendered-pose reconstruction, not hand-drawn per-frame art.
- Recommendation: pending

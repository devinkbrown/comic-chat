# Task contract: CC-20260722-32

## Outcome

Bring Reinked's live Comic Chat message serializer into byte-level agreement
with the Microsoft source, then record and implement any additional bounded
wire gaps discovered in the source audit.

## Route

- Score: `3 + 2 + 3 + 2 = 10`
- Primary lane: `frontier`
- Required reviewer: `frontier`
- Maximum parallel lanes: `1`
- Critical domain: `IRC/IRCX/UDI wire compatibility`

## Ownership

- Owned paths: `src/main.zig`, `src/net/client.zig`, `src/comic/session.zig`,
  `src/proto/udi.zig`, `docs/PROTOCOL.md`, `docs/audits/`
- Read-only dependencies: Microsoft `v2.1b/cchat/{ircproto.cpp,protsupp.cpp,ircsock.cpp}`; `docs/WORKERS.md`
- Forbidden paths: `legacy/`, generated assets, TLS implementation
- Base commit: `2eca85b`

## Evidence and constraints

- Microsoft `protsupp.cpp:546-563` formats `# Appears as <name>.` and passes
  it to the regular Comic Chat message serializer.
- Microsoft `ircproto.cpp:171-390` embeds annotations in `PRIVMSG` while
  comic data is enabled; IRCX `DATA` is only the source's alternate mode.
- Preserve verified TLS, bounded queues, and receive-side DATA compatibility.

## Acceptance criteria

- [ ] Avatar and colored-avatar announcements are source-shaped PRIVMSG bytes.
- [ ] Normal comic chat emits one embedded-UDI PRIVMSG, including with IRCX.
- [ ] Source-audit findings have source-file-and-line evidence and focused tests.

## Verification

- Focused gate: `zig build test --summary all`
- Aggregate gate: `zig build test`
- Build gate: `zig build`
- Hygiene gate: `git diff --check`

## Authority

- Local edits authorized: `yes`
- External writes authorized: `no`
- Destructive actions authorized: `no`
